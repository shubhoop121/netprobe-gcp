server {
  listen __PORT__;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html index.htm;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # TODO: Add a /api proxy pass to the backend API service
  # location /api {
  #   proxy_pass $API_SERVICE_URL; 
  # }
}